# Simple Query Router – Python Track

This project is a simple CLI-based Python application that routes user queries
to different mock agents (GitHub, Linear, Jira) based on intent.

## How to run
```bash
python main.py
